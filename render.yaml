services:
  - type: web
    name: retireshield
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NEXT_PUBLIC_BASE_URL
        value: https://retireguard.onrender.com
