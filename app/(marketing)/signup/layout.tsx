import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create your RetireShield account",
  description: "Save your Retirement Safety Score, create your account, and continue to your dashboard.",
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
